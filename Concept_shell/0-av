#include <stdio.h>

/**
 * main - prints all arguments without using ac
 * @av: argument vector
 *
 * Return: 0
 */
int main(int __attribute__((unused)) ac, char **av)
{
	int i = 0;

	while (av[i] != NULL)
	{
		printf("%s\n", av[i]);
		i++;
	}

	return (0);
}
